XGBOOST_PARAMS = {
    'n_estimators': 100,           # Número de árboles (default es 100)
    'max_depth': 6,                # Profundidad máxima de los árboles (default es 6)
    'learning_rate': 0.3,          # Tasa de aprendizaje (default es 0.3)
    'subsample': 1.0,              # Fracción de datos usados por cada árbol (default es 1.0)
    'random_state': 0,             # Semilla para reproducibilidad (default es 0)
    'colsample_bytree': 1.0        # Fracción de características usadas por cada árbol (default es 1.0)
}
