SVR_PARAMS = {
    'C': 1.0,                # Regularización (default es 1.0)
    'epsilon': 0.1,          # Margen de tolerancia (default es 0.1)
    'gamma': 'scale',        # Coeficiente del kernel (default es 'scale')
    'kernel': 'rbf'          # Tipo de kernel (default es RBF)
}
